CREATE TABLE player (  
	id SERIAL PRIMARY KEY,
	username TEXT,
	
);

CREATE TABLE game (  
	id SERIAL PRIMARY KEY,
	
);

CREATE TABLE game_round (  
	id SERIAL PRIMARY KEY,
	
);

CREATE TABLE round_seat (
	
);

CREATE TABLE round_action (  
	id SERIAL PRIMARY KEY,
	
);
